# 问题日志

<!-- 格式说明
## YYYY-MM-DD HH:MM | 类型（build / unit / runtime）
- 触发命令/工具：
- 失败摘要：
- 关键日志：
- 处理动作：
- 修复变更：
- 复跑结果：
- 关联条目：
-->

## 2026-05-18 00:00 | unit
- 工具：CMake Build + CTest (`ctest -C Debug --output-on-failure`)
- Preset/Target：Debug / `ui_tests`
- 失败摘要：`BatchManagerTest.DifferentPushConstantsBreakBatchMerging` — 期望 2 个批次（不同 opacity），实际合并为 1 个批次
- 关键日志：
  ```text
  D:\test\PMK\tests\unittest\ui\test_BatchManager.cpp(98): error: Expected equality of these values:
    batchManager.getBatchCount()
      Which is: 1
    2U
      Which is: 2
  [  FAILED  ] BatchManagerTest.DifferentPushConstantsBreakBatchMerging (0 ms)
  ```
- 初步定位：`src/ui/managers/BatchManager.hpp` — `beginBatch()` 合并条件只比较 `texture + scissorRect`，未比较 `pushConstants`；而测试期望 `opacity` 差异（1.0F vs 0.5F）应阻止批次合并
- 处理动作：已记录待人工判断——**语义/设计分歧**，需确认 `opacity` 是否应当作为批次合并的判断维度：
  - 生产代码注释明确写"只比较纹理 + 裁剪区域"，并将逐实体字段提取到顶点属性（`m_pendingQuadParams`）
  - 若 `opacity` 已写入顶点属性，批次可以合并（测试预期有误）
  - 若 `opacity` 仍靠 GPU push constants 驱动，则不同 opacity 不能合并（生产代码遗漏了对 pushConstants 的比较）
- 修复变更：无（升级给用户）
- 复跑结果：未复跑（等待人工确认后方向）
- 关联条目：无