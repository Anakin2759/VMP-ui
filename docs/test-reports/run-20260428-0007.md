# 测试构建闭环报告 — 2026-04-28 00:07

## 概览
| 阶段 | 结果 | 用时 | 备注 |
|------|------|------|------|
| Configure | ➖ | 未执行 | 复用现有 build/ 目录；未重新 configure；缓存确认 generator=Ninja、CMAKE_BUILD_TYPE=RelWithDebInfo、ENABLE_BUILD_TESTS=ON |
| Build | ✅ | 一次通过 | target=all；StateSystem 拆分相关改动后全量构建通过 |
| Unit | ✅ | 87/87 通过 | 当前未发现 CTest labels；按可用 CTest 全量执行，其中非 IntegrationTest 命名用例均通过 |
| Integration | ✅ | 4/4 通过 | 未发现独立 integration 标签；全量 CTest 中包含 4 个 UiIntegrationTest 命名用例，均通过 |
| Coverage | ➖ | 未执行 | 当前复用的是 RelWithDebInfo 构建目录，未启用覆盖率构建 |

## 失败用例（如有）
无。

## 覆盖率热点（未覆盖 Top 5）
1. 未生成覆盖率报告；当前构建未启用覆盖率工具链。

## 本轮自动修复
- 无。

## 待人工处理
- [ ] 无。

## 补充说明
- 本轮按照要求优先复用了现有 build/ 目录，没有重新 configure。
- 全量构建通过，构建输出显示重新编译并成功链接了与本轮背景最相关的 UI、客户端与 UI 测试目标。
- CTest 工具输出为逐条单测执行日志，总体结果为全部通过；项目当前未提供可见的 unit/integration 标签信息，因此本报告按测试命名约定补充说明 IntegrationTest 覆盖情况。
- RunCtest_CMakeTools 输出中重复出现 DartConfiguration.tcl 缺失提示，但 CTest 返回码为 0，且所有测试均为 Passed；本轮不按失败处理。
- 问题日志.md 本轮新增条目数：0。